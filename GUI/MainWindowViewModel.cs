namespace GUI
{
    public class MainWindowViewModel
    {
        public MainWindowViewModel()
        {
     //       IClient client = new ConsoleClient();
     //       client.HandleClient();
        }
    }
}